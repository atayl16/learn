topic:
  key: config_secrets
  name: Config & Secrets Management
  track_key: deployment_infrastructure
  depth_target: Strong
  prerequisites: ["rails_basics", "security_basics"]
  objectives:
    - Manage environment-specific config with ENV variables
    - Use Rails credentials for secret management
    - Implement secret rotation strategies
  key_terms: ["ENV variables", "Rails credentials", "secret rotation", "12-factor app", "encryption"]
  est_minutes_read: 10
  est_minutes_exercise: 20
  tags: ["security", "config", "secrets", "rails"]
